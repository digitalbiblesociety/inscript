/**
 * Pinned text ids that the starter pack ships with. Update this list if the
 * starter-pack contents change.
 *
 * Used by e2e tests so we don't hardcode specific ids in multiple places.
 */

export const KNOWN_TEXTS = {
  // Adjust to a text id present in your starter pack.
  // The codebase uses the FCBH/DBS convention: 3-letter ISO 639-3 language
  // prefix + version code, uppercase, no separator (e.g. ENGKJV, ENGWEB,
  // SPNRVG). The dev config defaults to ENGWEB.
  primary: 'ENGKJV',
  // A short, distinctive phrase to validate search hits.
  searchPhrase: 'In the beginning'
};
